var tired = false; //Boolean variables register as either true or false;
var hungry = true;

var isTrue = 1 == 1; // "=" reads "is" (assigns info to a variable); "==" reads "is equal to" (evaluates value, returns T or F); 
console.log(isTrue);

var isNotTrue = 1 === "1" // "===" evaluates BOTH, VALUE and TYPE;
console.log(isNotTrue);


//  Operator	Meaning
//	==			equality 
//	===			strict equality
//	!=			inequality
//	!==			strict inequality
//	>			greater than
//	>=			greater than or equal to
//	<			less than
//	<=			less than or equal to
	
//--------------------------------------------------------
//--------------------------------------------------------
// ------ Challenge: Boolean Logic  ----------
//--------------------------------------------------------
//--------------------------------------------------------
/*---------------------------------------------------------*/


/*CHALLENGE: Bronze
Create 5 boolean variables that would be used in everyday programming
Using the boolean variables created above integrate logical and comparison operators to return true and false statements in the console.
*/

var eggplant = true; 
var overUnder = (age > 18) //var age = 100
var evaluationOfInput = userInput == Alaska //common case userInput; answer code
var verification = (var once === var tres) //where var's once & tres are previously assigned
var multiples = false;


/*CHALLENGE: Silver
Write your own conditional that deals with Boolean Operators and Boolean Logic. Get as complicated as you want, 
just make sure it runs. 
Sign up for an account on Repl.io. Add the code there and send it to James, Paul, and Kenn. 
You can do that on Slack by using the @ symbol for all of us.
*/

var facebookStatus = 0

if( facebookStatus < 5) {

}
	numberOfTags > 5) {
	return false
}
else {
	return true
}

var  numberOfTags = 4

var complicatedProblems = numberOfTags >= 1

if (complicatedProblems = true) {
	console.log("It's complicated.")
}


/*CHALLENGE: Gold 
I want to order some Colts gear online(Jona, go ahead with the Bengals(boo)), and I'm not sure 
which courier service to use yet. I want to know which one to use
for Next Day Air, 2 day air, 3 day ground, and whenever. Can you write
an if/else statement that helps me pick the best one? It's just your opinion here.
*/

var nextDayAir = true;
var twoDayAir = true;
var threeDayGround = true;
var whenever = true;

if(nextDayAir == true) {
	console.log("USPS Next Day priority")
} else {
	if(twoDayAir == true)
}









